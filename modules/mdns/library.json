{
  "name": "MdnsModule",
  "version": "0.1.0",
  "description": "ESPRack module: mDNS responder + resolver. Broadcasts <device-id>.local on the LAN and exposes IMdnsResolver for *.local hostname/service lookup. Adds a System / mDNS tab.",
  "license": "MIT",
  "frameworks": [
    "arduino"
  ],
  "platforms": [
    "espressif32"
  ],
  "dependencies": [
    {
      "name": "ESPRack"
    }
  ],
  "build": {
    "srcDir": "src",
    "includeDir": "include"
  }
}
